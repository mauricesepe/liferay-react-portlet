/**
*
* ContentPlaceholder
*
*/

import styled from "styled-components";

export default styled.div`
  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  animation: fadeIn 0s ease-in-out ;
`;
